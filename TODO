ALL:
env variables flag.StringVar(&Consul_URL, "consul-url", LookupEnvOrString("CONSUL_URL", Consul_URL), "service discovery url")
	flag.StringVar(&Statsd_URL, "statsd-url", LookupEnvOrString("STATSD_URL", Statsd_URL), "statsd's host:port")
	

github:

- github auth
- rate limit tracking and  polling frequency
- test (at least one ) (may need to move the api to ghpr struct )
- add healthchek for kubernetes liveness and readiness go routine httpd  ?


slack:
- oauth2 
- catch error

